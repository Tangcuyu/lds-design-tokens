{
  "name": "lds-design-tokens",
  "version": "0.0.1",
  "description": "Implementation of LDS Design Tokens using Style Dictionary",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./build.js",
    "watch": "chokidar 'src/**/*.json' -c 'node ./build.js'"
  },
  "author": "Tangcuyu",
  "license": "MIT",
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "handlebars": "^4.0.11",
    "style-dictionary": "^3.0.0"
  }
}