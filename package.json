{
  "name": "@itsi/lds-design-tokens",
  "version": "0.0.1",
  "description": "Implementation of LDS Design Tokens using Style Dictionary",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./build.js",
    "watch": "chokidar 'src/**/*.json' -c 'node ./build.js'",
    "clean:dist": "rm -rf ./dist && mkdir -p ./dist"
  },
  "author": "Tangcuyu",
  "license": "MIT",
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "handlebars": "^4.0.11",
    "style-dictionary": "^3.0.0"
  },
  "dependencies": {
    "@divriots/style-dictionary-to-figma": "^0.4.0"
  },
  "publishConfig": {
    "registry": "http://localhost:4873"
  }
}